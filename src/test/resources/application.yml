spring:
  datasource:
    url: jdbc:hsqldb:hsql:/localhost/iyzicodb
    driverClassName: org.hsqldb.jdbcDriver
    username: root
    password:
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: create-drop
      use-new-id-generator-mappings: false